class Solution {
    public int[] sortedSquares(int[] nums) {
        int a = nums.length;
        int[] ans = new int[a];
        int b = 2;
        for(int i=0;i<nums.length;i++){
            ans[i] = nums[i]*nums[i];

        }
        Arrays.sort(ans);
        return ans;
    }
}
